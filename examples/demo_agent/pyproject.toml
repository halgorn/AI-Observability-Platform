{
  "name": "ai-obs-demo",
  "version": "0.1.0",
  "description": "Demo agent that emits events to AI Observability Platform",
  "requires-python": ">=3.10",
  "dependencies": [
    "ai-obs-sdk",
    "httpx>=0.27",
  ],
  "scripts": {
    "agent": "python agent.py",
    "replay": "python replay.py"
  }
}
